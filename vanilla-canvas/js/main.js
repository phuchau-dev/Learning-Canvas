import { ParallelogramAnimator } from './parallelogramAnimator.js';
document.addEventListener('DOMContentLoaded', () => {
    const animator = new ParallelogramAnimator('myCanvas');
    animator.start();
});
